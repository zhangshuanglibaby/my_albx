重点说明如何获取登陆详细结果

- 在数据库中字符串不区分大小写，所以不能在数据库中进行密码是否匹配的验证
- 在控制器中接收 邮箱和密码
- 将邮箱传递给数据模块，让数据模块根据邮箱查询出对应的数据(邮箱是唯一值，所以查询的结构要么没有结果集，要么有就一条数据)
- 控制器接收返回的数据，进行判断
  - 如果有结果集，说明根据邮箱查询到了数据，邮箱输入正确，再进行密码是否匹配的判断
    - 如果密码匹配，说明登陆成功
    - 如果密码不匹配，说明密码输入错误
  - 如果没有结果集数据，说明邮箱输入错误
